import React from 'react';
import PropTypes from 'prop-types';
import { CityWrapper } from './City.styled';

const City = () => (
 <CityWrapper>
    City Component
 </CityWrapper>
);

City.propTypes = {};

City.defaultProps = {};

export default City;
